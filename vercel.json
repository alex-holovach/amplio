{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "comments": "Production site deploys from apps/www (Next.js + /r registry). Use the deploy-site workflow or: pnpm sync-registry && pnpm --filter @useamplio/www build && cd apps/www && vercel deploy --prod --scope aholovach"
}
