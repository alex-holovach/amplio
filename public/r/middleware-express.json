{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "middleware-express",
  "title": "Express Middleware",
  "description": "Express middleware that attaches request-scoped logcn context.",
  "type": "registry:lib",
  "dependencies": [
    "@logcn/core"
  ],
  "devDependencies": [
    "@types/express",
    "express"
  ],
  "files": [
    {
      "path": "registry/middleware/express.ts",
      "target": "~/middleware/express.ts",
      "type": "registry:lib",
      "content": "import type { NextFunction, Request, Response } from \"express\";\nimport {createRequestLogger, runWithLogger, type Logger, useLogger} from \"@logcn/core\";\n\n\ndeclare global {\n  namespace Express {\n    interface Request {\n      logcn?: Logger;\n    }\n  }\n}\n\n\nexport function logcnMiddleware() {\n  return (req: Request, res: Response, next: NextFunction) => {\n    const requestLogger = createRequestLogger({\n      method: req.method,\n      path: req.path,\n    }).set({\n      http: {\n        method: req.method,\n        path: req.path,\n        route: req.route?.path,\n        ip: req.ip,\n        user_agent: req.get(\"user-agent\") ?? undefined,\n      },\n    });\n\n    req.logcn = requestLogger;\n\n    runWithLogger(requestLogger, () => {\n      res.on(\"finish\", () => {\n        if (requestLogger.sealed) {\n          return;\n        }\n        requestLogger.set({\n          http: { status: res.statusCode },\n          status: res.statusCode,\n        });\n        requestLogger.emit();\n      });\n\n      next();\n    });\n  };\n}\n\nexport function useRequestLogger(req: Request): Logger {\n  return req.logcn ?? useLogger();\n}\n"
    }
  ]
}
