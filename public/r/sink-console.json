{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "sink-console",
  "title": "Console Sink",
  "description": "Console sink that prints wide events as JSON.",
  "type": "registry:lib",
  "docs": "Wire the sink: add `import { consoleSink } from \"./sinks/console\"` to telemetry/logger.ts and append `consoleSink` to the init() sinks array. Prefer `npx @useamplio/cli@alpha add sink console` — it wires logger.ts for you.",
  "dependencies": [
    "@useamplio/amplio@^0.1.0-alpha.11"
  ],
  "files": [
    {
      "path": "registry/sinks/console.ts",
      "target": "~/telemetry/sinks/console.ts",
      "type": "registry:lib",
      "content": "import type { LogRecord, Sink } from \"@useamplio/amplio\";\n\nexport const consoleSink: Sink = (record: LogRecord) => {\n  console.log(JSON.stringify(record));\n};\n"
    }
  ]
}
